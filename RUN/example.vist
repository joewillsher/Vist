

func lt: Int Int -> Bool = do
    return $0 < $1

func or: Bool Bool -> Bool = do
    return $0 || $1


func fact: Int -> Int = |a| do
    if a <= 1 do
        return 1
    else do
        return a * fact(a - 1)
